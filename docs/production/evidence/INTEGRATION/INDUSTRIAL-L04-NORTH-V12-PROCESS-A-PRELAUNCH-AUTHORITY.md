# Industrial L4 North v12 Process-A prelaunch authority

- **Integration base:** `ffb3db1a35aec5067a07a5405ee721ff379ecd51`
- **Task / direction:** `PLAY-027` / North
- **Claim SHA-256:** `b7eb42ccacf323a3149a4c25faa587a0e6557afb6784d08e19fbe9d108e9434a`
- **Stage:** zero-child Process-A execution preparation
- **Production selected:** false

## Purpose

Create the real high-level Process-A execution boundary before Integration
publishes a live `prelock_north_a` schedule. The accepted schedule consumer
currently validates and returns a grant plan but intentionally cannot launch
Blender. This slice adds the separately reviewable one-child orchestrator and
Blender entrypoint that a later exact schedule will bind.

## Frozen inputs

| Binding | SHA-256 |
|---|---|
| North v12 scene | `dad20722f4770c82992040861074188c604b46cd226e5f739291ac22683594e2` |
| North v12 materials | `e683feed89f6878903d1ec0b255d0d5e8a36c74f431a2fb723287bf955c54d09` |
| lowering contract | `21480cd8c1dbb66f33b3ccd4987fea169198f2640b793a220d8d22c9c8505aa8` |
| v12 Blender importer | `ec726d584ce4b22253fca486e82bc6a198616debed94358d76f6dac7a1f62988` |
| accepted static-B v03 contract | `9a7449cdc76116bdd12879e17f99e8162828ba6c01507310a627816f9202d08f` |
| schedule-adapter contract | `50367dbfe9ffcc586ceb23c2af3d52344ea1f5c86958d82414c77d8334eeed58` |
| schedule consumer | `0ecd683b42896fa09414f0730240f43857d6aac3248acd720653393138812ad1` |
| deterministic readiness evidence | `df412064bb5499b7c72b1a087580e8a26d19adef54495fa24923c81f39a4dd8d` |

## Authorized task-owned surfaces

- `Native/CitySimNative/WorldArt/Blender/PLAY-027/industrial-l04-north-art-v12/process-a-execution-v01/`
- `docs/production/evidence/PLAY-027/industrial-l04/l04/blender-north-art-v12/process-a-execution-v01/`
- the existing North schedule-adapter files only where required to bind the new
  high-level orchestrator and current claim hash.

## Required implementation

Author, but do not execute:

1. an immutable Process-A execution contract;
2. one high-level launcher that first consumes the exact published schedule
   through the accepted North adapter, requires a `north:A` grant plan, and
   starts exactly one child only after every binding passes;
3. one Blender child entrypoint that imports the accepted v12 lowered scene,
   configures the governed Cycles camera/light/color/alpha/render settings, and
   writes only the future immutable Process-A output root;
4. no-DCC adversarial and static tests proving missing/stale schedule, wrong
   grant/slot/queue/root/orchestrator, dirty/untracked schedule, reused output,
   extra child, B/C, sibling, network, ImageGen, normalization, shipping, and
   low-level bypass all fail before child start; and
5. a deterministic prelaunch packet generated byte-identically in two distinct
   fresh roots with all child/DCC/render/pixel counters equal to zero.

The high-level launcher must contain exactly one child-start site, use a new
process group, cap the single child to one CPU thread, enforce the published
time and RSS envelope, kill the process group on violation, retain exact
command/provenance/terminal resource accounting, and reject retry or an
existing output root. The child entrypoint may contain the future render call
but must not run during this slice.

Update the North schedule consumer so a future schedule binds the high-level
launcher by exact path and SHA-256; the consumer remains unable to invoke that
launcher. The later schedule plus execution authority are both required.

## Required return

Return a clean focused checkpoint with exact changed paths and hashes; complete
test inventory; two-root prelaunch identity; AST/process-start proof; exact
frozen-input bindings; and explicit counts of zero Blender, DCC, render,
source-pixel, normalizer, ImageGen, sibling, Renderer, and shipping activity.

## Stop boundary

Do not start Blender or any child. Do not create a source pixel, `.blend`,
normalization output, source packet, appearance lock, production profile,
source admission, Renderer receipt, runtime/atlas/manifest/package change, or
shipping selection. Do not edit sibling roots, shared schedule controls, push,
integrate, or self-accept.

After independent prelaunch acceptance, Integration may publish the exact
validator-passing `prelock_north_a` schedule and a separate one-process
execution authority. That later Process A must stop after one fresh North
source candidate for independent literal-scale and technical review.
