# PLAY-027 Industrial L4 Blender North art v04 authority

**Returned candidate:** `ba2c41270092263367ef2b259c70518317dc8deb`

**Passing v03 zero-pixel checkpoint:** `ccef83d455d0cb537d61acdb1d8e8acc74cd4ba3`

**Disposition:** `AUTHORIZED_VISIBLE_ROAD_COURT_REPAIR`

North v03 proves the intended foundry, compact staff entrance, portal value
hierarchy, raw technical intake, and analytic portal-to-socket connectivity.
Preserve those results.

Literal review nevertheless returns the appearance lock. The rendered apron
still reads as a slab extending away from the governed North socket. The
descriptor's socket-side connector passes analytic connectivity only because
the foundry mass occludes it. Hidden geometry is not player-visible frontage
and cannot satisfy the family lock.

## Preserve exactly

- the broad hall, four-monitor roof rhythm, stack/process bay, three-tier
  silhouette, warm control wing, and current material hierarchy;
- the portal void, jambs, header, reveals, compact `15 × 13` envelope, zero
  process overlap, and staff entrance's complete `4 × 8` assembly;
- CONTRACT-020 camera, true-ground registration, footprint, pivot, North
  socket, light, contact/shadow direction, Cycles settings, transparent canvas,
  padding, alpha/chroma, and hidden-RGB behavior;
- every v01/v02/v03 scene, raw, failed probe, diagnostic, review, and
  disposition artifact unchanged.

## Authorized v04 repair

World Art may create one North-only v04 scene/material revision and task-owned
validator/review updates. Change only the portal-side exterior court/apron
topology required to make the already accepted portal visibly road-served.

The repair must:

1. place the primary concrete road court entirely on the portal's exterior
   side of the hall instead of underneath the preserved building mass;
2. expose one continuous compact apron component from the visible portal
   threshold to the governed North socket neighborhood in the final rendered
   RGBA—an occluded semantic connector does not count;
3. keep the first visible apron pixels directly beneath/outside the freight
   portal and make the path narrow toward the socket, so it reads as a freight
   entrance rather than a foundation slab or a road baked into the asset;
4. preserve the portal and staff entrance locations unless actual-camera proof
   shows that the portal itself is on the wrong socket-facing plane; if so,
   stop before pixels and return that evidence to Integration rather than
   silently redesigning the facade;
5. add an actual-camera visible-material proof that the rendered apron color
   component touches the portal threshold, survives without building
   occlusion, and reaches within two compact pixels of the socket;
6. include a deterministic review surface compositing the exact source over
   the governed 2:1 footprint and North road-edge/socket guide, in addition to
   source, native-2x, literal-192 color/grayscale, silhouette, registration,
   and accepted-L3 comparisons; and
7. introduce no massing, roof, stack/process, staff, broad palette, camera,
   registration, crop, chroma, padding, hidden-RGB, or portal-occlusion
   regression.

## Binding cadence

1. Commit one v04 zero-pixel descriptor and visible-material proof checkpoint.
2. Stop without pixels if the portal plane itself contradicts the North socket.
3. Otherwise render exactly one new North process A at the unchanged governed
   settings.
4. Build the required review packet and stop for independent technical and
   literal-scale appearance review.

If process A passes both reviews, CONTRACT-021 revision 2 permits Integration
to publish a non-production appearance lock and release North B/C plus
East/South/West A/B/C concurrently. Until then, B/C, sibling pixels,
normalization, renderer quarantine, shipping, production selection, and family
staged-app QA remain blocked.
