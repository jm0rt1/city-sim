# Industrial L3 West source-v02 repeat stop

West was rendered exactly three times from the frozen source-v02 descriptor,
material library, renderer binary, and sampling contract. Runs A and B are
file- and decoded-pixel-identical. Run C differs from both by two RGB pixels
within source bounds `[640, 657, 643, 658]`; alpha differs at zero pixels.

All three outputs are complete and share exact occupied bounds, visible-pixel
count, registration, and alpha. The locality sheet binds the split to one
three-pixel-wide row without changing the complete West silhouette or
frontage.

This is the final ordered first-failure stop. West was not normalized and the
four-direction catalog/review packet was not assembled. No sampling repair is
requested. The accepted North, East, and South immutable masters and their
normalized LODs remain unchanged. `sourceAuthority=false` and
`productionSelected=false`.
