# Rejected compact attempt

`compact-victory-modal-attempt.png` and its AX tree are retained as a failed proof attempt. The terminal `story-commercial-charter-victory-v1.json` state correctly opened the Town Charter blocking result, so the obscured frame is not used as Commercial skyline evidence.

The accepted compact packet instead uses the active, paused `story-commercial-complication-v1.json` fixture.
