# Rejected PLAY-073 visual attempts

These frames are retained as negative evidence and are not candidate proof.

- `m1`: small terrain-patch iterations remained sparse.
- `m1b`: circular/lobed breakup read as stamped spots.
- `m1c`: organic breakup removed the worst spots but remained visually weak.
- `m1d`: a repeated grass fill exposed giant diamond/road-like artifacts.
- `m1e`: the first district-ground pass cropped authoritative ends.
- `m1f`: frontage plus an over-tight camera still cropped the district.
- `m1g`: strong regular default direction retained in the product.
- `m1h`: strong compact scale, but north/south district ends remained under HUD.
- `m1i` and `m1j`: trusting the supplied compact safe aperture produced a
  toy-island city and failed the composition gate.
- `m2`: per-empty-cell vacant context exposed the simulation grid as colored
  squares and was removed completely.

The failed variants do not define shipping geometry, assets, camera behavior,
or acceptance.
