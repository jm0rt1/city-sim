# Rejected duplicate-LOD attempt

The two retained frames have identical SHA-256
`6a1c54d082e0b467291375f23f2baa16566fce20cdea3fde333ab090074c3b1a`.

They are rejected because the process attached by Computer Use did not contain
`CITYSIM_DATA_ROOT`, `CITYSIM_REGULAR_WINDOW`, or
`CITYSIM_PROOF_CAMERA_SCALE`. The filenames therefore did not represent
different camera states. No product conclusion uses these images.

All binding baseline routes were recaptured after launching with
`open -n --env ...` and checking the exact PID environment.
