# PLAY-075 R4-F exact-candidate final QA

## Disposition

`RETURN`

Exact candidate `fc996a287f5d0d16f58c13bc6ee2abadf4d64972` / tree
`f4c1814b848e706716db2f01d7b29cf13ba84a13` completed its sole isolated
fresh-player Computer Use launch. No rebuild, second journey, product repair,
candidate substitution, author coaching, push, integration, or pin occurred.

The main road/curb/parcel fabric is materially clearer, the regular and compact
command layouts remain strong, Focus City preserves critical truth, and the AX
surface is unusually complete. Those positives do not clear the frozen
automatic-return boundary.

## Release blockers

1. Maximized regular composition measures `690 / 1277 = 54.03%` connected
   district/public-realm width, below the required `60%`. Maximized Focus City
   reaches `63.59%`, proving the failure is the regular maximized camera/layout
   composition rather than missing candidate bytes.
2. A conservative connected-green measurement that discards every pixel inside
   the entire connected-district bounding rectangle still measures `32.64%` to
   `61.96%` of the safe aperture across the governed layouts, above the `25%`
   limit everywhere.
3. Detached edge residences, the lower service structure, and the water tower
   lack a clearly continuous road-facing frontage. Repeated orange/dark-roof
   residences and mixed civic-versus-context fidelity remain player-visible.
4. Pointer-selecting occupied block `6,8` for Road correctly exposed a blocked
   decision, demolition recovery, and Escape cancellation, but simultaneously
   announced `Road construction approved`. The false success message contradicts
   the blocked decision and unavailable map action.

## Evidence

- `RESULT.json` contains the exact route/candidate/bundle identity, disclosed
  measurement rules, five-layout matrix, interaction/AX results, screenshot
  hashes, and compact defect packets.
- `01-regular-opening.png` and `02-regular-frozen.png` show the real regular
  opening.
- `03-compact-window.png` is the exact `900x600` content layout (outer capture
  includes the macOS frame at `901x653`).
- `04-compact-focus-city.png`, `05-maximized-regular.png`, and
  `06-maximized-focus-city.png` prove the camera/layout contrast.
- `07-maximized-reduce-motion.png` proves composition with Reduce ambient
  animation enabled.

The candidate process was quit after capture and the matching process count
returned to zero. Integration owns any subsequent disposition or reroute.
