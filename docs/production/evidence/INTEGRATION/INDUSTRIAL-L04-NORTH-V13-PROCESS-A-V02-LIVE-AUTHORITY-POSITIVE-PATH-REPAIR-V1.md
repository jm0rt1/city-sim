# Industrial L4 North v13 Process-A v02 live-authority positive-path repair

**Disposition:** Integration `RETURN` of exact candidate
`533524ec869e81d6d80c4a1548e5613b6a0d898d`

**Worker parent:** `cffa19a528d1192794dea31a99dc4eb6db29579a`

**Claim:** `docs/production/claims/PLAY-027.world-art.md`, revision 10,
SHA-256 `bf0b167a1d1e6f7007d609aeb657917fe9d3d0866d5a7a6e36b0e5a32faefa6f`

**Classification:** `LUNA_LOCAL_DEBUG / gpt-5.6-luna / max`

## Accepted bytes and retained boundaries

The candidate's revision-10 current-authority rebind, exact eight-path scope,
frozen six-input set, fixed Blender command, one-child ceiling, outside-output
attempt marker, atomic consumption, committed schedule-blob equality,
child-side repository checks, direct-child rejection, nonblocking capture, and
33-adversary zero-DCC proof remain the repair base. This return does not reopen
the North design, lowering, family vocabulary, schedule design, source-art
judgment, or any sibling direction.

No schedule, process receipt, attempt marker, DCC process, Blender child,
render, source pixel, normalization, Process B/C, appearance lock, source
admission, Renderer activation, production selection, or shipping mutation is
authorized by this repair.

## Reproduced blocker

The current `_changed_paths()` compares the live worktree to execution base
`cffa19a528d1192794dea31a99dc4eb6db29579a` and includes every untracked file.
`preflight()` then rejects every path outside the worker's eight allowed files.
The later Integration-direct launch requires an Integration-published schedule,
process receipt, and attempt marker, while the schedule publication commit must
be an ancestor of the live worker HEAD. Those exact legal authority files are
therefore rejected before `prepare_integration_launch()` can validate them, so
the accepted launcher cannot reach `launchReady` under its own contract.

The committed `HANDOFF.json` and `ORCHESTRATOR-READINESS.json` also are not
proved byte-equal to the objects returned by `build_documents()`.

## Frozen repair

Repair only the prospective `process-a-v02` boundary:

1. Preserve the strict worker-delta audit. Do not whitelist the Integration
   namespace, a directory prefix, arbitrary untracked files, or arbitrary
   descendants of master.
2. For the live-authority path, validate the exact normalized schedule and
   process-receipt paths and bytes, the exact committed schedule blob and
   publication commit ancestry, and the exact available attempt marker before
   excluding only those three validated authority paths from the worker-delta
   audit.
3. Use the validated schedule publication commit as the Integration side of
   the delta boundary, or an equivalently strict construction that proves the
   live HEAD consists only of the accepted worker candidate plus that exact
   published authority. After the exclusion, every remaining changed path
   must still be one of the exact eight worker paths.
4. Preserve the existing zero-authority preflight behavior: without the exact
   schedule, receipt, and marker, the lane remains validation-only and starts
   no child.
5. Add a positive isolated fixture with real Git publication/blob ancestry and
   exact schedule, receipt, and available marker bytes. It must reach
   `launchReady == true`, return the one fixed command, report
   `childStarts == 0`, and never call `subprocess.Popen`, Blender, DCC, or the
   child.
6. Add negative fixtures for a namespace-neighbor file, unvalidated exact-path
   file, forged publication blob, wrong publication commit, wrong receipt,
   wrong marker, extra untracked file, replayed marker, and path alias. Each
   must fail before subprocess or output-root creation.
7. Generate the deterministic readiness and handoff objects through the owned
   implementation and assert their canonical committed bytes are exactly the
   generated bytes. Preserve truthful `sourceAuthority:false`,
   `productionSelected:false`, and zero Process-A/pixel activity.

## Exact allowed paths

- `Native/CitySimNative/WorldArt/Blender/PLAY-027/industrial-l04-north-art-v13/process-a-v02/EXECUTION-CONTRACT.json`
- `Native/CitySimNative/WorldArt/Blender/PLAY-027/industrial-l04-north-art-v13/process-a-v02/RUNNER-CONTRACT.json`
- `Native/CitySimNative/WorldArt/Blender/PLAY-027/industrial-l04-north-art-v13/process-a-v02/launch_north_v13_process_a_v02.py`
- `Native/CitySimNative/WorldArt/Blender/PLAY-027/industrial-l04-north-art-v13/process-a-v02/render_north_v13_process_a_child.py`
- `Native/CitySimNative/WorldArt/Blender/PLAY-027/industrial-l04-north-art-v13/process-a-v02/test_process_a_v02.py`
- `docs/production/evidence/PLAY-027/industrial-l04/l04/blender-north-art-v13/process-a-v02/HANDOFF.json`
- `docs/production/evidence/PLAY-027/industrial-l04/l04/blender-north-art-v13/process-a-v02/ORCHESTRATOR-READINESS.json`
- `docs/production/evidence/PLAY-027/industrial-l04/l04/blender-north-art-v13/process-a-v02/CURRENT-AUTHORITY-REBIND.json`

Every `process-a-prelaunch-v01` byte, frozen design/lowering input, sibling
direction, shared Integration authority, Renderer/shipping surface, and legacy
source remains forbidden.

## Focused gate and stop condition

Run the v02 focused test twice from fresh isolated roots, validate all changed
JSON, and run `git diff --check`. Commit one coherent descendant of
`533524ec869e81d6d80c4a1548e5613b6a0d898d` with zero DCC, child, Process-A,
render, and pixel activity. Stop immediately on a path outside the eight-file
claim, a shared-contract decision, a candidate/authority mismatch, or a second
unsuccessful repair attempt. Integration owns independent review, any actual
schedule publication, launch, aggregate gate, acceptance, integration, push,
and final staged-app QA.
