# Smallest evidence-backed repair proposal — not authorized or executed

Create one new pre-pixel Industrial L2 East source revision only after
integration approval. Preserve the exact external silhouette, camera,
registration, materials, lighting, shadows, sampler, and compositor while
removing the overlapping volume that gives two materials ownership of the
visible `z=25` plane.

The narrow topology repair is to decompose
`i02-east-high-assembly-hall` around the unchanged
`i02-east-process-tower` volume:

- left hall strip: `x=-25…-23`, `z=-13…25`;
- right hall strip: `x=-7…13`, `z=-13…25`;
- rear hall block behind the tower: `x=-23…-7`, `z=-13…11`;
- process tower remains `x=-23…-7`, `z=11…25`.

This preserves the union, height, footprint, and visible outline while giving
the tower sole ownership of the brick segment on the positive-z facade.
Before pixels, extend the task-owned structural validator to reject two
outward material owners on any camera-visible coincident plane. Then run only
the East three-process raw gate. Stop if any SceneKit 4× full-frame or final
raw identity still splits; do not add thresholds, filtering, raster repair, or
normalization.

This is a proposal only. It does not authorize source-v06 or any descriptor
mutation.
