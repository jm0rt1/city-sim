# PLAY-027 Industrial L2 V04 North primary capability rejection

Disposition: `REJECTED_PRE_PIXEL_CAPABILITY_GATE`.

The single authorized North process acquired the approved Apple M5 Pro Metal
backend, decoded the frozen V04 descriptor and immutable East v05 material
library, and then stopped during task-owned offline scene construction:

```text
unsupported prop kind: explicit-box
```

`explicit-box` is an intentional authored primitive for the two exact North
HVAC-bank volumes. Removing or replacing those descriptor nodes would change
the independently approved geometry and is not authorized.

No SceneKit snapshot, raw PNG, or raw provenance record was emitted. The
backend capability record is retained as the process-level provenance. West
was not invoked. South, repeats, normalization, source authority, and
production selection remain blocked.

This is an offline-pipeline capability failure, not an art rejection. The
smallest separately authorized repair is one task-owned `explicit-box` branch
in `OfflineSceneRenderer.addProp`, using the existing deterministic
`boxNode` path after rejecting non-positive or non-three-component
dimensions. No descriptor, material, sampling, camera, registration, or
shipping/runtime behavior may change.

`rawPixelCount=0`, `westProcessCount=0`, `sourceAuthority=false`, and
`productionSelected=false`.
