# PLAY-027 Industrial L3 North/West frontage authority

- Exact returned worker checkpoint:
  `e5613a3faa34b35c15385e465e4e47f17cf88b5e`
- Integration disposition:
  `AUTHORIZE_NORTH_WEST_PREPIXEL_FRONTAGE_REPAIR`
- Authorized directions: North and West only
- Authorized stage: pre-pixel descriptor and review proof only
- East and South: immutable
- Raw render authorization: `false`
- Source authority: `false`
- Family authority: `false`
- Production selected: `false`

The checkpoint closes the normalized-chroma return. All twelve LODs are unique,
file- and decoded-pixel-identical across two runs, and contain zero exact or
near chroma at every nonzero alpha. Alpha, padding, registration, accepted
catalog bytes, and the warm/dark A0 treatment remain intact.

The literal compact overlay proves an art defect: North and West place the
authoritative road edge, socket, and door-base segment correctly, but foreground
mass occludes the loading faces. Only dark upper slivers survive beneath the
ochre fascia. East and South expose the intended three loading faces plus staff
entrance and remain immutable references.

World Art may now author one constrained North/West geometry revision that:

- preserves the governed footprint, pivot, road socket, door-base midpoint,
  northwest light, southeast shadow, height envelope, material-role library,
  logical identity, and East/South bytes;
- exposes at least two complete dark loading-bay rectangles and a separately
  readable staff entrance in each North and West compact-neighborhood view;
- keeps the loading/entrance frontage at least eight pixels wide and within two
  pixels of the authoritative road socket;
- retains the tall plant volume, roof equipment, process mass, grounded plate,
  and family recognition rather than flattening the building into a facade;
- creates no coincident structural planes, cross-direction alias, runtime
  transform, recolor-only repair, or footprint/padding expansion; and
- produces color, clay/grayscale, compact projected frontage, road/socket
  overlay, and N/E/S/W silhouette comparisons from descriptors only.

Commit the exact pre-pixel candidate and stop for independent integration
review. Do not render raw pixels, normalize, alter East/South, touch renderer or
shipping surfaces, begin Industrial L4/A2, push, or self-accept.
