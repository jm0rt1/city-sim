# Industrial L3 East immutable-master checkpoint

Published disposition `8a1126a1f18984be6994c8e177ba4289063cbeef`
designates the retained source-v02 East run A as the exact direction-specific
immutable master under `CONTRACT-018`.

The raw is not rerendered. Its file, decoded-RGBA, descriptor, and material
hashes match the published authority. Two fresh no-Metal normalizer processes
produce byte-identical and decoded-pixel-identical block, neighborhood, and
city LODs. All three LODs have unique identities, transparent padding, zero
hidden RGB, zero exact chroma, and zero visible magenta spill.

The retained East repeat failure remains immutable evidence. This checkpoint
does not grant four-view Industrial L3 source authority: South and West remain
ordered repeat gates. `productionSelected` remains false.
