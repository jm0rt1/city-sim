# PLAY-027 Industrial L2 East v04 single-primary disposition

**Disposition:** Rejected at the literal raw technical gate. This is not
source-art acceptance, production selection, or authority for another process.

Exactly one integration-authorized fresh Metal-visible process was consumed
from clean authority
`39ee0b196ce4f11b38d7ab54b1c540e25aa3882d`. The exact committed probe and
review tools compiled with warnings as errors. The committed synthetic
alpha-relation proof replayed at SHA
`8e259ddf68e830aebfa31127e89f63eb3716dbe9d8068e6c2cc25ce663efb326`.
The historical v04 tooling-failure rejection remained byte-preserved.

The one process emitted the governed raw, genuine post-Lanczos pre-chroma
RGBA, registered building and building-plus-shadow alpha proofs, neutral
composite, provenance, metrics, and the complete raw-only review panel set.
The corrected per-pixel alpha relation passes:

- zero-alpha input maps to exact opaque `#ff00ff`;
- foreground input and output alpha hashes are identical;
- relation violations, hidden RGB, near-magenta opaque pixels, and proof
  magenta-family pixels are all zero.

The binding stop is the separate literal
`alpha-visible-bounds-equal-rgb-bounds` gate. The raw non-chroma RGB bounds are
`[509,488]...[1028,905]` (`520x418`), while the genuine pre-chroma registered
building-plus-shadow alpha bounds are `[511,490]...[1024,895]` (`514x406`).
The width ratio is `0.9884615384615385`, so exact equality is false. No gate
was relaxed or reinterpreted.

All other recorded technical targets pass: registration; complete rendered
node bounds; `514` source / `144.5625` native-2x building span; `422` source /
`118.6875` native-2x core span; `17.15625` native-2x minimum feature; p25
`114`; IQR `62`; p95 `201`; seven occupied step-32 bins; maximum major-facade
bin share `0.34305070981713187`; and identity-bearing minimum bin `112`.

Literal panel inspection shows the intended wide-low campus, three separate
loading doors, pale roof/concrete, blue-gray hall/process forms, and restrained
ochre safety hierarchy in color and grayscale. That observation does not
override the failed technical gate and is not self-acceptance. Independent
visual disposition remains pending.

No repeat, B/C, other direction, normalization, LOD, source repair, ImageGen,
production selection, shipping/runtime/shared mutation, or second Metal
process was performed. `productionSelected` remains `false`.
