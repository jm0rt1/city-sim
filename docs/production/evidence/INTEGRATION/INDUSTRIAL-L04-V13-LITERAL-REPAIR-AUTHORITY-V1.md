# Industrial L4 v13 literal-repair authority

**Owner:** Integration

**Status:** active frontier design authority

**Execution boundary:** `LUNA_IMPLEMENTATION / gpt-5.6-luna / high`

## Exact reviewed inputs and disposition

Integration independently reviewed the synchronized zero-pixel candidates
against the frozen Portal Crown Foundry family, canonical cameras and sockets,
and literal-192 thresholds. The following exact inputs remain durable and are
returned, not accepted:

- North implementation input
  `aaea9fda4249e88a82791d9a418408575e5f32ff` is returned solely because the
  rejected `process-a-prelaunch-v01` current-tree module still exposes a
  caller-keyed fixture-authority builder and consumer. PLAY-027 revision 9 is
  the complete bounded retirement authority. Preserve every `process-a-v02`
  byte and do not run DCC or Process A.
- East candidate `ca3f1978691ec6d01589751f14c2bcc71962fd94` passes its
  35 declared adversaries and two deterministic zero-pixel runs, but it does
  not measure or enforce its declared 60 x 58 source-pixel whole-building
  envelope. Independent exact-camera projection is only approximately
  57.14 x 38.18 pixels; its portal is effectively at the 20-pixel minimum,
  freight beats are approximately 5.14 pixels wide, and crown separation is
  approximately 3 pixels. It is returned for one East-only literal-scale
  repair with real post-quantization headroom.
- South candidate `27a79304bf17997ce09721d85f860fd0233cd5bf` preserves
  correct socket, portal, crown, occlusion, and zero-pixel behavior, but its
  freight beats measure approximately 2.857 x 8.427 pixels against the frozen
  4 x 8 minimum, its staff entry measures 4 x 8.999 pixels against the 5 x 8
  minimum, and it serializes family identity as `industrial-l04` instead of
  canonical `industrial_l04`. It is returned for those three South-only
  corrections and adversaries.
- West candidate `0bb1bb49c195249221be82b37b68991bde015230` preserves a
  strong 19 x 28 clear portal, 28 x 38 outer portal, 37-pixel crown, and four
  silhouette breaks, but it has one freight aperture instead of three readable
  freight beats with two separators, its staff glazing is 4 x 8 pixels instead
  of at least 5 x 8, and its validator omits the freight, staff, and stack-share
  authority checks. It is returned for one West-only semantic/literal repair.

These returns are direction-local. No passing sibling proof may be removed,
weakened, or demoted because another direction failed.

## Frozen repair outcomes

### East — PLAY-079 revision 9

Within the existing East-exclusive v13 source and evidence roots only:

1. Measure and record the exact-camera whole-building occupied bounds after
   source-pixel quantization.
2. Enforce at least 60 x 58 pixels and add adversaries for missing measurement,
   undersized width, undersized height, and stale declared-versus-measured
   values.
3. Strengthen portal, freight, and crown geometry enough that the complete
   packet retains clear headroom after quantization rather than merely touching
   the frozen minima.
4. Preserve the immutable East camera, socket, pivot, footprint, material
   vocabulary, non-alias boundary, and all currently passing adversaries.

### South — PLAY-080 revision 9

Within the existing South-exclusive v13 source and evidence roots only:

1. Increase every governed freight beat to at least 4 x 8 source pixels and the
   staff entry to at least 5 x 8 source pixels after exact-camera quantization.
2. Normalize the exact family identity to `industrial_l04` everywhere in the
   current v13 packet.
3. Add below-threshold, missing-measurement, stale-measurement, and wrong-family
   adversaries while preserving the passing socket, portal, crown, occlusion,
   and no-alias proofs.

### West — PLAY-081 revision 9

Within the existing West-exclusive v13 source and evidence roots only:

1. Author and prove three separately readable freight recess beats with at
   least two source-pixel separators while preserving the accepted portal and
   crown hierarchy.
2. Increase governed staff glazing to at least 5 x 8 source pixels after
   exact-camera quantization.
3. Enforce freight-beat count, separator width, staff minimum, and stack-share
   authority in the validator with missing/below/alias adversaries.

## Shared stop boundary

This authority is zero-pixel only. It authorizes no Blender/DCC child, render,
normalization, source pixel, A/B/C process, appearance lock, source admission,
Renderer activation, production selection, shipping mutation, push,
integration, self-scoring, or self-acceptance. A worker stops on any required
shared contract, sibling path, subjective visual decision, or failure outside
its exact direction. Integration performs the independent technical and
literal-scale disposition after each clean candidate returns.
