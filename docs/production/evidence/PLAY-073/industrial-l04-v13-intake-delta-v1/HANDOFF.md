# PLAY-073 Industrial L4 v13 renderer intake delta

This is a candidate-neutral, evidence-only preparation checkpoint for the
North-v13 design vocabulary and the four future Industrial L4 direction rows.
It does not admit source art, inspect pixels, activate runtime lookup, mutate
an atlas/manifest, or authorize a staged app or QA journey.

## Bound renderer seam

`RESULT.json` preserves the existing renderer intake logical IDs
`industrial_l04_v0_{north,east,south,west}`, authoritative CitySim sockets,
three semantic LOD slots per direction, and the fixed orthographic camera,
footprint, pivot, and no-transform registration contract. The v13 portal
thresholds are expectations copied from the published North design authority;
they are not pixel or appearance acceptance claims for any direction.

Each direction remains `prepared_blocked_waiting_source_admission` with null
source-packet, admission, and quarantine locators. Any missing, extra,
authority-drifted, aliased, mirrored, rotated, transformed, fallback, socket,
LOD, or symlink-escaping input must fail only that direction and preserve
already accepted siblings byte-for-byte.

## Four-direction trigger

The join stays `blocked_zero_live_source_admissions` / `quarantined_incomplete`.
Only exact `{north,east,south,west}` receipts, 12 unique LOD identities, and
32 unique D4 identities may transition it to
`ready_for_atomic_assembly_nonshipping`; the corrected fourth receipt invokes
the existing assembler exactly once. Inputs remain empty and all activation,
shipping, production-selection, app, and QA flags remain false.

## Focused proof

Run the route-local intake-plan validator, JSON validation of `RESULT.json`,
and `git diff --check`. Integration owns the aggregate Swift/build gate and
QA owns the single player-facing journey. The next legal step is consumption
of exact Integration source-admission receipts; no worker may manufacture or
admit them here.

Stop on any identity mismatch, shared-contract decision, path outside
`docs/production/evidence/PLAY-073`, or subjective/acceptance judgment.
