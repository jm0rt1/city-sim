# PLAY-073 R4-F4/F5 Visual Return Repair Authority

## Exact returned evidence

Independent PLAY-075 returned product candidate
`fc996a287f5d0d16f58c13bc6ee2abadf4d64972` at clean evidence commit
`452420feb8379fc386f84fdf81663ac65be93207`. The exact packet is
`docs/production/evidence/PLAY-075/r4-f-fc996a28-final-v1/RESULT.json`.
The main connected road/curb/parcel fabric passed, overlap/fallback passed,
and the regular and compact command systems passed. Two remaining visual
defect families are disjoint from the R4-F3 camera repair and from each other.

## R4-F4 district-fabric return repair

Own only:

- `Native/CitySimNative/Sources/CitySimNative/Rendering/TerrainRenderer.swift`
- `Native/CitySimNative/Sources/CitySimNative/Rendering/RoadRenderer.swift`
- `Native/CitySimNative/Tests/CitySimNativeTests/PLAY073DistrictFabricTests.swift`
- `docs/production/evidence/PLAY-073/r4-f4-district-fabric-return-v1/`

Replace the remaining broad, connected plain-green visual mass with authored,
coherent terrain/public-realm structure that remains truthful and buildable.
Do not hide terrain by cropping, opaque screens, fixture-only geometry, or
invented occupancy. Use one restrained material language shared with the
accepted roads, curbs, sidewalks, parcel grounds, and northwest-light/
southeast-contact direction. Vacant land must remain legible as buildable
opportunity, not as a flat repeated cell plate.

Focused pixel evidence must use the exact regular, `900x600` compact, compact
Focus City, maximized regular, and maximized Focus City apertures. With the
frozen QA classifier, the conservative largest connected green component
outside the complete district bounding rectangle must be at or below `0.25`
in every layout. Preserve the already passing connected main fabric, zero
seams/fallback, topology, hit geometry, placement truth, LOD behavior, and
deterministic two-run identity.

## R4-F5 place-cohesion return repair

Own only:

- `Native/CitySimNative/Sources/CitySimNative/Rendering/LotContextRenderer.swift`
- `Native/CitySimNative/Sources/CitySimNative/Rendering/LotRenderer.swift`
- `Native/CitySimNative/Tests/CitySimNativeTests/PLAY073PlaceCohesionTests.swift`
- `docs/production/evidence/PLAY-073/r4-f5-place-cohesion-return-v1/`

Make every occupied or context structure read as grounded and road-facing.
The upper-left and far-right residences, lower service structure, and water
tower require a continuous entrance/frontage, parcel-ground, and contact-
shadow relationship to their authoritative connected road without changing
logical topology or hit geometry. Break the orange-masonry/dark-roof
residential stamp through deterministic, truth-bounded context composition,
silhouette support, material/value variation, and frontage treatment. Bring
residential and service context fidelity into the accepted City Hall/park
language without replacing or transforming accepted directional art.

Focused evidence must show the exact returned locations in color and
grayscale at city, neighborhood, and block LODs, plus deterministic repeat
identity. Require zero detached frontage, zero adjacent context alias,
unchanged sockets/pivots/selection, and no new overlap or fallback.

## Shared stop and acceptance boundary

R4-F4 and R4-F5 may run concurrently and must not edit each other's paths or
`CityScene.swift`. They may not change accepted source art, resources,
simulation, gameplay, UI, persistence, commands, claims, shared contracts, or
build scripts. Each returns one coherent commit and compact machine-readable
result. Integration joins R4-F3/F4/F5, runs the full Swift suite and staged
build once, and creates one exact candidate. Only a new independent PLAY-075
real-app journey may accept visual quality; focused masks, tests, and worker
prose cannot substitute for that judgment.
