# Industrial L3 A0 cohesion family candidate

**Disposition:** `BLOCKED_NORTH_WEST_COMPACT_FRONTAGE_ART_FAILURE`.

This packet proposes no source, family, production-selection, renderer,
or shipping acceptance. It binds four separately authored N/E/S/W
source-v04 treatments: immutable accepted East calibration plus North,
South, and West derived from each direction's own accepted source-v02
geometry and registration. No sibling transform, mirror, rotation,
post-process tint, recolor-only alias, or fallback is used.

The family contains four unique raw identities and twelve unique
normalized identities. Every normalized LOD is byte- and decoded-pixel
identical across two fresh normalizer processes, with zero hidden RGB,
exact chroma, or visible magenta spill and passing padding and
registration. Accepted Residential, Commercial, Industrial L1, and
Industrial L2 catalog intersections are empty.

The strict task-owned post-resize canonicalizer repairs the returned
normalization defect: all twelve LODs are unique, exact across two
runs, and contain zero exact or near chroma at every nonzero alpha,
with alpha, padding, registration, and accepted catalog bytes
preserved.

The literal compact-neighborhood road/socket overlay reveals an art
defect rather than a proof omission. East and South retain three full
dark loading faces plus the staff entrance. North and West place the
authoritative cyan road edge, white door-base segment, and yellow
socket correctly, but foreground mass hides the loading faces; only
dark upper slivers beneath the ochre fascia survive. The candidate
therefore stops before geometry mutation exactly as required by the
published return. Source authority, family authority, and
`productionSelected` remain false.
