# PLAY-027 Industrial L4 CONTRACT-019 R2 authority

- Published integration base: `16666881ce399250d220c8c63a3d2f2360fe4796`
- Returned matte candidate: `6a71795973955db0647f7cd531c6b9bb8ae89a82`
- Rejected semantic checkpoint: `3822fa4b13ab7cdfff5232dd5058b1109e30c0ba`
- Source authority: `false`
- Production selected: `false`
- Authoritative raw processes: `0`
- Diagnostic SceneKit/Metal processes authorized: `2`

## Independent disposition

The matte output and retained evidence are correct, but the reusable stage is
returned because it trusts caller-provided decoded, descriptor, material, and
palette assertions. The smallest repair computes and binds those facts from
the actual bytes and invokes its admission path against all 44 accepted
masters. Output pixels should remain unchanged.

The custom semantic rasterizer is rejected from the accepted candidate. It
duplicates the renderer it is meant to measure and failed before emitting
panels, metrics, or replay proof. Preserve checkpoint `3822fa4b` only as
historical branch evidence.

## Authorized recovery

World Art may:

1. repair the matte-v2 binding and negative tests;
2. remove the rejected semantic rasterizer files from the candidate;
3. add one exact-candidate semantic-visibility contract beside the existing
   task-owned offline diagnostic contracts;
4. narrowly extend the task-owned `OfflineSceneRenderer` to replace named
   v17 nodes with unique constant semantic materials after the real
   `ContractSceneBuilder` runs;
5. execute exactly two diagnostic-only SceneKit/Metal processes through the
   existing 4x snapshot, governed Lanczos, registration, and scale path; and
6. decode those outputs into component metrics and actual-scale comparison
   panels without reimplementing projection or rasterization.

Required component groups are both portal jambs, header, inset/void, hall,
gantry, and crucible/occluder. Required scales are source, native-2x, and
literal 192. A/B inventories must be byte-identical.

Stop and return before modeling if the existing v17 actual path fails any
CONTRACT-019 visibility gate. If it passes mechanically but remains visually
weak, return the panels for independent review rather than self-accepting it.

No authoritative raw, B/C, sibling, normalization, ingestion, shipping, or
production-selection process is authorized.
