# PLAY-027 Industrial L3 A0 family disposition

- Exact worker candidate:
  `03b0ffc1211eb8d4cbefa5676671b004cc03605c`
- Integration disposition: `RETURN_FOR_NORMALIZED_CHROMA_AND_FRONTAGE_PROOF`
- Source authority: `false`
- Family authority: `false`
- Production selected: `false`
- Renderer or shipping mutation: none

The A0 family is materially better than returned R2
`b4191d98ee7c526bc08a6fe272521588572e27fd`. Independent inspection confirms
that its warm concrete, charcoal structure, oxidized metal, dark dock depth,
and restrained ochre now fit the accepted warm/dark catalog. N/E/S/W retain
authored directional silhouettes, registered geometry, stable pivots and
sockets, distinct raw and normalized identities, and recognizable city LODs.

The exact candidate remains returned for two narrow reasons:

1. exact magenta survives in normalized pixels at nonzero alpha: East city
   `(156,116)` alpha `2`, East neighborhood `(202,184)` alpha `1`, and three
   North plus three West neighborhood pixels at alpha `2–3`; and
2. North and West preserve the orange frontage fascia at compact scale, but
   their actual loading-bay or entrance meaning is sufficiently occluded that
   socket-correct road-facing frontage is not yet proven.

Before a replacement R2 ingestion window opens, World Art must:

- preserve all four raw masters, directional geometry, logical keys, pivots,
  sockets, shadows, and accepted catalog bytes;
- repair only the task-owned offline normalization/canonicalization path so
  every normalized output contains zero exact or near chroma at every alpha
  greater than zero;
- regenerate all twelve LOD outputs twice and prove file plus decoded-pixel
  identity;
- add an exact compact-neighborhood N/E/S/W road-edge and authoritative-socket
  overlay in which the real door or loading-bay frontage—not merely its accent
  stripe—survives; and
- bind per-direction median and p95 luminance, saturated-accent share, and
  nonzero-alpha chroma counts into the unified family manifest.

If the overlay reveals an art defect rather than a proof omission, World Art
must stop before changing geometry and return that exact defect to integration.
Industrial L4 and opening-variant work remain blocked until this family is
accepted and the focused replacement R2 publishes.
