# Industrial L4 direction pre-lock repair authority

- **Authority:** Integration
- **Stage:** zero-pixel runner and validation preparation
- **Pixel production:** blocked
- **Shared-surface mutation:** blocked

The accepted East, South, and West predesigns remain valid. This authority
releases independent preparation that is contract-separable from the returned
North appearance redesign.

## Common derived non-alias input

All three cells must consume the Integration-owned
`industrial-l04-accepted-master-non-alias-input-v1.json`. They may not invent a
direction-local catalog or omit accepted non-shipping Industrial L3. Candidate
decoded RGBA must be rejected if it intersects the canonical 44-master
forbidden set. This derived input grants no source, renderer, shipping, or
production acceptance.

## East — PLAY-079

East may bind its existing zero-pixel runner and future source handoff writer
to `industrial-l04-source-stage-handoff-schema-v1.json`, add a schema
validation command/receipt, and prove missing/stale appearance authority fails
before any renderer launch. No pixels or A/B/C process may run.

## South — PLAY-080

South may:

- correct the malformed 65-character Blender fingerprint in its runner
  contract to accepted 64-character SHA-256
  `8485107307b16bd0899f3c259261494b0c80e383db239c04e2c9fcd14d305fb4`;
- replace the literal-192 placeholder with deterministic measurement from the
  direction-local semantic proof so the existing five-field validator can
  actually pass;
- bind the common source-stage schema and non-alias input; and
- rerun static/describe/guard/schema gates only.

No Blender render, ImageGen, normalization, contact-sheet, or A/B/C process is
authorized.

## West — PLAY-081

West may:

- change the execution ancestry guard to require the accepted integrated v06
  proof `3d76fab8a45807c34198a6d8bb1dd1eeff7be51e` while retaining
  `3e01ca6738d7574718f9aeff4b66771eee109feb` as source-candidate provenance;
- replace the unprovisioned Pillow dependency in the task-owned validator with
  a direction-local standard-library PNG decoder;
- bind the common source-stage schema and non-alias input; and
- rerun static/describe/guard/schema gates only.

No Blender render, ImageGen, normalization, contact-sheet, or A/B/C process is
authorized.

## Return contract

Each direction returns its own clean, focused commit and task-owned handoff.
One cell failing does not stop the others. Workers may not edit this authority,
the shared ledger, a sibling root, the common schema/input, or any renderer or
shipping surface.
