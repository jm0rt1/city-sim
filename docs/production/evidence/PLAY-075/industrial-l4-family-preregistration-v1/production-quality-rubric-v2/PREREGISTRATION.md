# PLAY-075 production-quality rubric v2

- **Disposition:** `BLOCKED_UNMEASURED_AWAITING_EXACT_LIVE_CANDIDATE`
- **Published authority:** `b36bb1b1c680978328208716ca4a59caf8729f39`
- **QA merge carrier:** `d0b79769d1a75c4103fb3a9799c3dfb9dca38d3a`
- **Claim/lane:** `PLAY-075` / `codex/citysim-playtest-quality`
- **Candidate under test:** none

This is an additive candidate-neutral production-quality rubric. It does not
modify, replace, rescore, or relax the accepted Industrial L4 family 20/20
gate. The exact accepted rubric, preregistration, evidence plan, and final
staged-app contract are hash-bound in `RUBRIC-CONTRACT.json`.

Every live value is `UNMEASURED`. No app, source pixels, renderer candidate,
fixture derivative, product resource, or author assessment was inspected.

## Bound production-quality checks

### Cross-family cohesion

Candidate-bound composed frames must measure:

- substantial overlap in facade value ranges;
- saturated accent material below `10%` of opaque building pixels;
- neighborhood outline-weight difference no greater than `1` screen pixel;
- consistent northwest key light and southeast contact shadow with comparable
  contact-shadow density; and
- road/curb/parcel/building material-scale ratio within `0.75–1.33`.

The authority supplies no numeric overlap or shadow-density threshold, so this
packet does not invent one.

### Industrial L4 player recognition

At untouched literal `192` scale, a fresh unprompted viewer must recognize one
monumental road-served recessed freight portal within `2` seconds. It must
retain two supporting sides, a clear header, contiguous dark inset depth, and
separation from the gantry/crucible.

### Frontage and progression

Exact compact Neighborhood proof must retain:

- frontage width at least `8` pixels;
- grayscale contrast greater than `15` value points;
- socket distance no greater than `2` pixels; and
- L3-to-L4 silhouette-boundary change greater than `20%`.

### Comparison breadth and fragile-detail survival

Unlabelled color and literal-grayscale comparisons must include Industrial
L1, L2, L3, and L4 plus Residential L4 and Commercial L4 at native gameplay
and exact compact scales. Corrugation, windows, railings, roof equipment, and
texture must produce zero unstable subpixel-detail failures after
normalization.

### Dense-context repetition

The authentic opening must contain at least three materially different
architectural silhouettes among frequently repeated families/levels, zero
edge-adjacent repeats of the combined silhouette/roof/frontage/lot-treatment
signature, no more than two visible occurrences of one identity, and
candidate silhouette-mask overlap below `80%`.

### HUD, map, and layering

At regular and exact `900×600`, developed district/public-realm pixels must
occupy at least `60%` of safe width and the largest plain-terrain component
must occupy no more than `25%` of the safe aperture. Compact map aperture must
remain at least `58%` closed and `45%` open.

Priority, negative cashflow, utility, pause, and action truth must remain
readable in one glance. Compact Details must expose at least one complete
actionable section and two complete notice summaries. Pointer, keyboard,
menu, Command Guide, topmost-first Escape, Full Keyboard Access, AX,
VoiceOver, and Reduce Motion must preserve map, target, focus, action, and
truth without stale or hidden state.

### Governed state, persistence, and accessibility

The future exact candidate must retain early, pressured, recovered, upgraded,
and terminal states at regular and exact compact widths across City,
Neighborhood, and Block LODs: `5 × 2 × 3 = 30` state-view cells in color and
literal grayscale.

Save, terminate, relaunch, and load must preserve fingerprint, strategy phase,
next action, paused state, and selected target. Each state at each width must
retain pointer/keyboard target parity, Full Keyboard Access, AX and VoiceOver
identity, and Reduce Motion meaning: `5 × 2 = 10` state-width accessibility
bindings.

## Gate boundary

This packet authorizes no live work. Final disposition remains
`BLOCKED_UNMEASURED_AWAITING_EXACT_LIVE_CANDIDATE`. Integration must provide
and admit one exact candidate before any value may be measured or any
candidate result returned.
