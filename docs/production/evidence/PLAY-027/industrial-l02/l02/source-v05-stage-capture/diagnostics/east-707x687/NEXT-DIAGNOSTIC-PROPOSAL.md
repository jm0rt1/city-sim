# Narrow next diagnostic proposal — not authorized or executed

Split the remaining prequantized boundary without changing authored inputs:
for unchanged Industrial L2 source-v05 East, retain the immutable SceneKit 4x
RGBA support window that contributes to source coordinate `(707,687)` and the
current post-Lanczos prequantized 3x3 in exactly three fresh Metal-visible
processes.

The sole question would be:

- if the 4x SceneKit support window differs, classify the cause in SceneKit
  draw/coverage before software downsampling;
- if the 4x support window is identical but the post-Lanczos prequantized
  buffer differs, classify the cause in the software CI Lanczos stage.

Keep the descriptor, materials, geometry, camera, registration, constant
lighting, disabled SceneKit shadows, no-MSAA mode, 4x factor, quantizer,
canonicalizer, compositor, and every retained packet byte unchanged. This is a
diagnostic proposal only, not a repair, source revision, normalization, or
production-selection proposal.
