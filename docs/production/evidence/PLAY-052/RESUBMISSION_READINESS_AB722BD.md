# PLAY-052 corrective-wave resubmission readiness

**Authority:** accepted master `ab722bd1ea7c8c132525362bc94bc12d154a78f5`

**Quality lane:** `codex/citysim-playtest-quality`

**Status:** ready, awaiting two separate exact frozen candidate handoffs

No candidate is tested or scored by this record. Computer Use, product
interaction, and product disposition are prohibited until integration names
the exact candidate hashes and staged identities. Renderer and integrated
dispositions remain separate.

## Preserved accepted quality history

- Quality renderer rejection:
  `527994ca61fa6e0c9349774f131cadc80f96388f`
- Patch-equivalent accepted master commit: `4bbe72b`
- Quality corrected-UI timeout:
  `2fee1b2a5f1ff7000f4b1910510f274698f84f8b`
- Patch-equivalent accepted master commit: `643fd1a`

The renderer pair has stable patch ID
`69468e6960bc0fd6c694eb89661b6fcb0bdc1b9c` and identical blob IDs for
all 13 evidence files, including the JPEGs. The UI-timeout pair has stable
patch ID `7fef1f62f831a54a77ef19693413f3d502640b93` and the same disposition
blob. The quality branch was synchronized through a normal merge; neither
quality commit was rewritten.

## Gate A — corrected PLAY-022 renderer

### Handoff admission

Integration must supply all of the following before quality launches anything:

- exact product commit and clean renderer evidence HEAD, with product ancestry;
- exact staged bundle, manifest, executable, resource-bundle and generated
  resource-manifest paths plus SHA-256 values;
- exact candidate ID, bundle/preferences identity, isolated data root, save,
  seed, tick, camera, default and compact window, and display identity;
- one and only one exact executable PID, with an explicit cleanup owner;
- uncropped default and exact 900 x 600 real-app frames for city,
  neighborhood, and block stops, plus same-seed rejected/corrected comparison;
- frozen engineering results for full suite, staged verification, collision,
  fallback, residency, cold load, regular/compact memory, node/draw/action
  stability, hit testing, accessibility, and Reduce Motion.

Any missing or ambiguous identity blocks the gate. Author screenshots,
harness-only frames, another bundle, or the prior `2cf18b0` candidate cannot
substitute.

### Independent live matrix

Quality will operate the exact staged app at default and exact 900 x 600:

| Check | Required player-visible result |
|---|---|
| Developed frame | Developed content and truthful expansion context occupy 55–70% of the unobstructed world band without player reframing. |
| Physical composition | The district reads as connected city fabric rather than the rejected isolated crossroads or an empty field. |
| Road/terrain coherence | Terrain, pavement, shoulders, curbs, junctions, frontage and intentional termini form one physical system with no seam, ladder-marking clutter or unexplained end. |
| Three LOD stops | City communicates network and density; neighborhood communicates blocks and frontage; block communicates construction, contact and material detail. Each is materially useful and stable through continuous pan/zoom. |
| Physical overlap | No unintended building/building, building/road, ground/road, entrance/prop or interaction-mark overlap appears live. |
| Interaction restraint | Normal, pointer hover, keyboard selection, valid/invalid placement, construction, overlay and consequence states preserve the target and show rejection copy once. |
| Construction truth | Prepared, foundation/frame, finishing and completed states stay inside the declared parcel and share the production material/light language. |
| State truth | Selection, utility/consequence treatment and recovery remain legible, non-color-only and do not invent simulation facts. |
| Input and AX | Pointer hit testing, keyboard selection/action, semantic City map identity/actions and AX target descriptions agree. |
| Accessibility | Default/compact, Full Keyboard Access where available, grayscale/color-vision evidence and Reduce Motion preserve equivalent meaning. |
| Operational stability | Repeated LOD cycling shows no fallback, pivot/contact jump, continuing memory high-water, unbounded actions, or identity drift. |

Quality will retain candidate-specific screenshots, AX text, a command/action
log, continuous pan/zoom evidence, timing/memory disclosure, file hashes,
process proof, and exact cleanup.

### Frozen score and automatic rejection

Five categories are scored independently from 0–4:

1. composition and developed-frame occupancy;
2. projection, material, light, road and terrain coherence;
3. useful city/neighborhood/block LOD and depth;
4. believable life, state clarity and interaction restraint;
5. systemic shipping credibility and performance.

Acceptance requires **at least 17/20**, **no category below 3**, and every lost
point explained. Automatic rejection applies to any visible unintended
overlap, mixed art language, mostly empty city frame, unexplained road end,
obscured target/selection/overlay, duplicated rejection copy, silent fallback,
over-budget memory or continuing high-water growth, harness-only proof, or
another obvious user-quality failure.

The corrected candidate must retain the prior engineering strengths rather
than trade them for appearance: exact deterministic resource identity, zero
fallback, zero governed collision, passing pointer/keyboard/AX and construction
truth, compact and Reduce Motion behavior, 135/135 frozen native suite, the
five accepted cold totals (4.431, 4.196, 4.476, 4.527 and 4.341 ms), and the
accepted regular/compact memory ceilings. Quality will disclose the supplied
method and independently verify the new candidate identity; it will not replace
a governed series unless integration explicitly commissions a rerun.

## Gate B — integrated PLAY-015/038/045 candidate

### Handoff admission

Integration must supply:

- one exact integration product commit containing the frozen PLAY-015,
  PLAY-038 and PLAY-045 product commits and accepted PLAY-014/037/043/044
  dependencies;
- exact staged bundle, manifest and executable SHA-256, candidate ID,
  bundle/preferences identity, isolated data root and save paths;
- zero stale same-executable processes followed by one sole exact PID whose
  executable and environment match the manifest;
- candidate-specific fresh-start and won-state fixtures, including a declared
  durable recovery route and a valid backup-only save;
- accepted automated and staged-verification results without treating them as
  the player disposition.

No master substitute, lane bundle, stale PID, author-operated journey or
harness-only outcome is admissible.

### No-coaching Charter journey

Starting from the authored opening, a player without developer instruction
must:

1. dismiss onboarding and establish map focus by pointer and keyboard;
2. understand the immediate pressure and commit a strategy through a real
   action;
3. diagnose the authored setback, choose one qualifying recovery deliberately,
   and explain its cost/tradeoff;
4. observe that the first qualifying recovery identity is retained and does
   not flip after later city changes;
5. satisfy the Town Charter standards and enter authoritative `.won` exactly
   once at the governed boundary, without premature award or required coaching;
6. explain why the city won, which strategy/recovery shaped it, and what Start
   a New Region will do.

The evidence ledger records timestamps for first meaningful decision,
consequence latency, confusion, dead time, false feedback, recovery, Charter
award, and replay initiation. The route rejects if the tester must be told
where to click, what counts, why a state changed, or whether victory occurred.

### Victory and replay matrix

| Surface | Required result |
|---|---|
| Victory truth | Copy celebrates the Town Charter result, never an inaccurate “thriving metropolis,” and names authoritative strategy/recovery analytics without inference. |
| Recovery identity | The chosen Commercial or Industrial resolution remains visibly distinct, exact and unchanged through victory, save/load, replay evidence and won-state relaunch. |
| Terminal state | Charter award and message occur once; won state is immutable under later ticks and cannot be entered before the qualifying payoff/check sequence. |
| Pointer replay | Start a New Region activates exactly once and produces the declared fresh-region result. |
| Keyboard replay | Deterministic initial focus plus Return and Space each activate the same existing action exactly once in separate resets. |
| AX replay | The victory surface and replay control expose truthful label/help/value/action; AX Press executes the same route exactly once. |
| Cancellation/focus | Escape and focus precedence remain topmost-first without leaking commands or silently resetting the city. |

### Persistence and backup-only Load matrix

1. Save the exact won city and record its state fingerprint, recovery identity,
   status, tick, primary/backup paths, sizes and SHA-256 values.
2. Terminate only the manifest-bound PID.
3. Relaunch the same bundle against the same explicit root in exact compact
   mode, prove the new sole PID and environment, then Load.
4. Require exact fingerprint/status/strategy/recovery equality, truthful won
   presentation, and paused simulation.
5. In a separate isolated root, remove the primary from availability while
   retaining a valid last-known-good backup. Load must remain enabled through
   menu, shortcut, toolbar and command guide, and must recover the exact city
   paused with truthful backup-recovery feedback.
6. Verify the valid backup is not silently promoted, deleted or replaced; keep
   before/after file hashes. Invalid backup-only and empty-root cases must stay
   disabled or reject without state mutation or false success.

The integration-owned `script/persistence_relaunch_gate.sh` is the preferred
identity/file-inventory harness once the exact manifest is supplied; live
pointer/keyboard/AX operation remains mandatory between its start and finish
phases.

### Default and exact compact truth

Both default and exact 900 x 600 must preserve:

- Charter victory copy, retained recovery explanation and reachable replay;
- pointer, keyboard, Full Keyboard Access and AX parity;
- semantic City map identity/actions when the map is available;
- deterministic focus, topmost-first Escape and command quarantine;
- unclipped critical controls and a truthful paused/running indication;
- save/relaunch/load and backup-only recovery feedback without hidden coaching.

### Gate B automatic rejection

Reject for any candidate/manifest/PID ambiguity, coaching, premature or
unclear victory, inaccurate victory copy, lost/flipping recovery identity,
double-fired or inaccessible replay, pointer-only operation, generic compact
map semantics, focus/Escape leakage, disabled backup-only Load, state or
fingerprint drift, unpaused load, false recovery feedback, altered/deleted
backup evidence, stale-process reuse, missing cleanup, or product changes made
by quality.

## Evidence separation and current state

- Corrected renderer evidence will be created under a new candidate-specific
  child of `renderer-gate-pending/` or an equivalent separate PLAY-052 renderer
  directory.
- Integrated victory/persistence evidence will be created under a new
  candidate-specific child of `integrated-gate-pending/`.
- Neither gate may inherit the other gate's disposition.
- Current status for every live criterion in this document: **not run —
  awaiting exact frozen candidate handoff**.
