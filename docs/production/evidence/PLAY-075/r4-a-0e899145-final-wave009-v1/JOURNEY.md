# PLAY-075 R4-A exact-candidate journey

## Boundary

The one-attempt lease was consumed once against exact product commit
`0e89914566ba4593b25e2cd52b4b788d204b7331`. No build, product resource,
fixture, preference domain, data root, or candidate substitution occurred.
The already-staged executable was launched once as PID `20602` and that exact
PID was terminated after capture. Industrial L4 was neither activated nor
scored.

## Fresh no-coaching route

1. The welcome surface explained the build/balance/diagnose loop without hidden
   coordinates. The journey entered through **Start Building**.
2. The player chose the Industrial strategy from the visible **Act** surface.
3. The first Industrial target was visibly invalid because road access was
   absent. The preview retained target, footprint, cost/upkeep, consequence,
   `Escape`, and a visible **Target adjacent road** recovery action.
4. Road access was created with keyboard and pointer variants. The first road
   action was undone successfully, then repeated through the alternate input.
5. Industrial was built at block `15,16`; the route committed and created the
   warned utility/pollution pressure.
6. **Utility capacity** opened progressive Details with exact power and water
   use/capacity/spare values. A Power Plant and Water Tower were built through
   the map-first recovery route.
7. The authoritative outcome reached **FREIGHT RESULT / STORY COMPLETE / Utility
   expansion locked in**. The city was paused at day 255 with treasury `$4,274`,
   `-$61/cycle`, residents `404`, happiness `58%`, jobs `282`, utilities `100%`,
   power spare `249`, and water spare `230`.

This was consequential and recoverable. The negative cashflow remained visible
rather than being hidden by the successful utility recovery.

## Input, layout, and accessibility segments

- Regular and compact captures retained the same paused recovered city. The
  regular decorated capture is `1278x768`; the compact app content is the
  governed exact `900x600` inside the uncropped Computer Use window envelope.
- City, neighborhood, and block LOD were traversed using the player-visible
  command guide's map-camera controls and captured at both widths.
- Pointer selection identified **Residential, block 10,11, Level 1,
  Operational**. With the same map target focused, keyboard `Shift-Return`
  opened the same identity and action surface.
- The map AX value announced completed/maintained condition, severe local power
  and water service, `60%` vitality, the exact primary action, and the same
  block coordinate. The visible Details surface preserved the city aperture.
- Full Keyboard Access `Tab` traversal exposed a visible focus indication and
  moved to the announced **Open New Arcadia command center** control.
- The command guide disclosed search, shortcuts, availability, and the
  topmost-first `Escape` rule. `Escape` closed the guide and then Details in the
  expected order.
- The Pollution overlay retained a compact legend and did not hide priority,
  selected target, speed, or map controls.
- macOS Reduce Motion was changed from its original `off` state to `on` only for
  the candidate segment. Priority, city map, utility action, command deck, and
  static visual meaning remained present. The original `off` setting was
  restored before process termination.
- **Save** produced the AX action update **City saved**. A second process start
  was forbidden by the exclusive lease, so no terminate/relaunch segment was
  attempted or claimed.

## Demolition and exact Undo

The selected Residential L1 at block `10,11` was demolished. The UI announced
**Structure demolished · Undo is available**. Undo restored the structure and
cleared selection. Re-selecting the same visible building reproduced the exact
normalized selected-target AX fingerprint:

`81416f6f3b30169d49631dc6ddf8f2889dbffeb9e46fa6f285f7528f87a835db`

The immediate post-Undo empty fingerprint file records the expected deselected
surface; `undo-restored-fingerprint.txt` is the authoritative restored-state
comparison and matches `undo-pre-fingerprint.txt` byte for byte after the
normalization header.

## Performance observation

The single live sample for exact PID `20602` was `239328 KiB` RSS, `0.8%` CPU,
and elapsed `33:16`. No visible hitch, accumulating overlay, input stall, or
resource fallback appeared during repeated layout, LOD, Details, overlay,
selection, demolition/Undo, and Reduce Motion transitions. The exact candidate
receipt's Integration-owned `325`-test result (`3` expected skips, zero
failures) and staged-build/resource identities were consumed; neither the full
suite nor the build was rerun.
