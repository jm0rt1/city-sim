# PLAY-027 Industrial L1 source-v02 frontage repair

Source-v01 remains rejected and immutable. Its exact crop showed complete
deterministic buildings but inadequate loading-bay readability on the
camera-far North and West frontage planes.

Source-v02 preserves:

- the four independently authored scenes;
- factory envelope, materials, sparse glazing, roof rhythm, and service props;
- exact footprint, pivot, directional socket, door base, light, and shadow;
- schema-2 v3 sampling and canonicalizer;
- `orientationTransform: none`;
- `productionSelected: false`.

The repair is task-owned loading-frontage geometry only:

- North now carries a full-scale roll-up return on the east-visible corner,
  attached to the authored north dock/canopy system.
- West now carries a full-scale roll-up return on the south-visible corner,
  attached to the authored west dock/canopy system.
- Both returns include concrete jambs/lintel, a full roll-up panel and slats,
  a grounded dock apron, and an industrial canopy.
- The primary entrance remains on the named far frontage. The visible return
  does not move the authoritative socket or replace it with a sibling scene.
- East and South retain their independently authored near-facing loading bays.

The exact source-v01 descriptors are archived under
`source-v01-rejected/descriptors/`. Source-v02 uses new scene geometry IDs and
source-revision binding; no rejected raw or provenance file is overwritten.

Pre-pixel validation passes with four unique descriptor hashes, four unique
geometry IDs, exact registration, zero coincident authored structural
boundaries, and unchanged accepted Residential/Commercial art.
