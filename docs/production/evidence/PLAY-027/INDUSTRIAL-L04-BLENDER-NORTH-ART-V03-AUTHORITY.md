# PLAY-027 Industrial L4 Blender North art v03 authority

**Returned candidate:** `3ff0b64180e0ce42f3c8261e3923f9131c23dafe`

**Synchronized World Art head:** `6040b5585d7b2b030b9a1265c8e4258b4fb191ef`

**Disposition:** `AUTHORIZED_FOCUSED_NORTH_FRONTAGE_REPAIR`

North v02 proves the governed pipeline and stronger art direction. Independent
technical review passed exact identity, scope, projection, true-ground
registration, pivot/socket, padding, alpha/chroma, component/material mapping,
portal envelope, and retained review hashes. Preserve those results.

Independent literal-scale review nevertheless returned the family appearance
lock. In `review/REGISTRATION-CONTACT.png`, the governed North socket is compact
`(112, 88)`, while `diagnostics/run-a/PORTAL-VISIBILITY.json` places the portal
at compact `x = 78...93`, `y = 86...99` and the apron at
`x = 65...90`, `y = 92...107`. The apron visibly extends away from the socket.
In `review/EXACT-192X128-GRAYSCALE.png`, the staff/control entrance disappears
and the portal merges with the adjacent dark facade.

## Preserve exactly

- the broad foundry hall, monitor-roof rhythm, stack/process bay, three-tier
  silhouette, and materially richer v02 identity;
- the accepted CONTRACT-020 Blender/Cycles toolchain, camera, true-ground
  projection, footprint, pivot, North socket, light and contact direction,
  transparent canvas, samples, and deterministic settings;
- the passing portal inset size, jamb/header envelope, zero occlusion, padding,
  alpha/chroma, hidden-RGB, object mapping, and material mapping;
- all v01/v02 scenes, raw pixels, diagnostics, review packets, and rejection
  records unchanged.

## Authorized v03 repair

World Art may create one North-only v03 scene/material revision that changes
only frontage-owned portal/apron geometry and compact frontage values.

The repair must:

1. make the apron travel from the portal threshold toward the governed North
   socket, with an actual-camera compact proof of a continuous connected path
   reaching the socket neighborhood instead of extending in the opposite
   direction;
2. preserve a monumental recessed freight portal while separating its void,
   jambs, header, apron, and adjacent facade in literal-192 color and grayscale;
3. make the staff/control entrance independently recognizable at literal 192
   through frame, reveal, canopy/light, or value hierarchy without competing
   with the freight portal;
4. preserve the v02 foundry massing, roof rhythm, stack/process silhouette,
   scale, footprint, contact, and all accepted registration/settings; and
5. introduce no sibling geometry, renderer/shipping/runtime changes, broad
   material redesign, new shared tool, crop, chroma, padding, hidden-RGB,
   portal-occlusion, or process-overlap regression.

## Binding proof and cadence

1. Commit one zero-pixel v03 descriptor checkpoint.
2. Run static and actual-camera portal/apron/socket connectivity, projection,
   occlusion, envelope, and registration proof before pixels.
3. Render exactly one new process A at unchanged governed settings.
4. Rebuild source, native-2x, literal-192 color/grayscale, silhouette,
   registration, and accepted-L3 comparison surfaces.
5. Stop for independent technical and literal-scale appearance review.

B/C, East/South/West pixels, normalization, renderer ingestion, shipping,
production selection, runtime changes, and family-lock publication remain
blocked. East/South/West zero-pixel predesign continues independently.
