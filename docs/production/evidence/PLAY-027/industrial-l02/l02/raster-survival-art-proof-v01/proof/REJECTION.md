# PLAY-027 Industrial L2 East raster-survival proof — rejected

The one authorized Metal-visible East process is complete and registered, but the literal pixels fail the binding art gate. This is a preserved non-source-authority visual rejection; `productionSelected` remains false.

Exact raw SHA-256: `77d47f3feef50c584dfd60177e41ab6793247a6772c536270806a0c3196db5b8`.

The building volume reaches the frozen world envelope and retains pivot `(768,896)`, East socket `(896,832)`, door base, 2:1 projection, and contact/shadow registration. Those technical facts do not overcome the visible failures:

- occupied-width utilization is `410 / 512 = 0.80078125`, not materially above the rejected 80.1% baseline;
- the frozen alpha treatment leaves 33,488 quantized magenta-shadow pixels, so the footprint field remains dominant;
- building luma has p25 `48`, median `48`, and p75 `54`, only a six-value interquartile spread despite isolated highlights;
- at source and native-2x the design reads as a small dark stacked box/tower;
- the three loading docks, canopies, and personnel route are not unmistakable without zoom;
- accepted Industrial L1 remains at least as legible in the bound color and grayscale comparisons.

The original renderer record is retained byte-exact. Its `rendererSourceCommit` argument contains a transcription error; `PROVENANCE-QUALIFICATION.json` binds the actual clean `920af3b8730a556c564daa56d9a0c9a4d451cf18` source tree and exact renderer-source/binary hashes without rewriting the record. No rerender is permitted or needed because the visual stop is independently binding.

No B/C process, N/S/W direction, normalization, LOD, source authority, shipping ingestion, or production selection follows this checkpoint.
